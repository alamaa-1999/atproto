---
'@atproto/pds': patch
---

Refuse records carrying self-labels other than `!no-unauthenticated` and `graphic-media`: Sunnahsky is strictly SFW, so the adult labels, and any label the app doesn't write, are refused from any client. Deletes are unaffected.
