{
  "extends": ["../../tsconfig/node.tsconfig.json"],
  "include": ["./**/*.ts"],
  "exclude": ["./node_modules"],
  "references": [{ "path": "../../packages/pds/tsconfig.build.json" }],
  "compilerOptions": {
    "rootDir": "./",
    "noEmit": true,
    // This directory is run by Node directly (`node index.ts`, see the
    // Dockerfile CMD) with no compile step, so a sibling import must name
    // the `.ts` file that actually exists on disk.
    "allowImportingTsExtensions": true,
    // Node's type stripping also refuses syntax that needs a compile
    // step, such as parameter properties, enums and namespaces. This makes
    // tsc reject it too, so the check fails before the container does.
    "erasableSyntaxOnly": true,
  },
}
